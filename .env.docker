# Production Environment Configuration

# Server Configuration
SERVER_PORT=8080
GIN_MODE=release

# Database Configuration (use Docker service names)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=change-this-strong-password-in-production
DB_NAME=taskmanagement

# JWT Configuration
JWT_SECRET=change-this-to-a-very-strong-random-secret-min-32-characters-production
JWT_EXPIRY_HOURS=24

# CORS Configuration
CORS_ALLOW_ORIGINS=https://yourdomain.com,https://www.yourdomain.com